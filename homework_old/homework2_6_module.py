def plus(x, y):
    from _decimal import Decimal
    """"""
    return Decimal(x + y)


def division(x, y):
    from decimal import Decimal
    """"""
    return Decimal(x / y)


def multiplication(x, y):
    from decimal import Decimal
    """"""
    return Decimal(x * y)


def minus(x, y):
    from decimal import Decimal
    """"""
    return Decimal(x - y)
